def gen_series(n):
    x=1
    ls=[]
    while x<=n:
        for i in range(1,n+1):
            ls.append(str(i))
        else:
            print("".join(sorted(ls,reverse=True)))               
        x+=1
